# Uncomment the next line to define a global platform for your project
platform :ios, '13.0'

target 'Alamofire-NetworkLayer-Combine' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Alamofire-NetworkLayer-Combine
pod 'Alamofire'


  target 'Alamofire-NetworkLayer-CombineTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'Alamofire-NetworkLayer-CombineUITests' do
    # Pods for testing
  end

end
